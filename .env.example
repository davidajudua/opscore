# Shared platform settings — used by the bots.
# Per-bot secrets live in bots/<bot>/.env.

LOG_LEVEL=
NODE_ENV=

# Optional: Discord guild ID to register slash commands against during development
# (omit in production for global commands).
DEV_GUILD_ID=
