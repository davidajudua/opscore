# Card Bot — card pool + /card claim flow
# Per-bot secrets only — shared platform settings live in <repo-root>/.env.

# Discord
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_GUILD_ID=

# Admin role allowed to run /load, /export, /whitelist, /providers, /purge, /stock, etc.
ADMIN_ROLE_NAME=
